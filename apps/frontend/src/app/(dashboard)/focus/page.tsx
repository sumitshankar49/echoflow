import { FocusModeView } from '@/features/focus/flows/view/focus-mode-view';

export const metadata = {
  title: 'Focus Mode · EchoFlow',
  description: 'Distraction-free Pomodoro timer with ambient sounds and music integration.',
};

export default function FocusPage() {
  return <FocusModeView />;
}
